<% provide(:title, 'Users') %>
<h1>Users</h1>

<%= will_paginate %>
<ul class="users">
  <%= render @users %>
  <% if @users.empty? %>
  	<p>Sorry, we couldn't find any users matching that name.</p>
  <% end %>
</ul>
<%= will_paginate %>