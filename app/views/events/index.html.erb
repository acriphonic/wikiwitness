<% provide(:title, 'All Events') %>
<h1>All Events</h1>
<% if admin_user?(current_user) %>
	<h2><%= link_to 'New Event', new_event_path %></h2>
<% end %>

<%= will_paginate %>
<ul class="events">
  <%= render @events %>
</ul>
<%= will_paginate %>